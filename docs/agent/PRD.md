# PRD · AI 备课助手（多 Agent 咨询模式）

> **版本**：v0.1（草稿，多轮迭代）
> **最后更新**：2026-05-26
> **状态**：🟡 起草中，需要回答 README.md 列出的"下一步问题"

---

## 一、Why · 为什么做这个

### 核心洞察

教练真正高频的痛点不是"排课"，是 **"会员有 X 状况，怎么调整训练"**。
排课助手解决了"记住课表"，但记住数据 ≠ 转化为洞察。

### 一句话定位

> 把教练大脑里"翻看上次记录 → 回忆原理 → 决定怎么排"这个动作，
> 用 AI agent 加速到 30 秒内有结构化建议。

### 价值假设（待用户访谈验证）

- H1：教练每次给老会员排课前，5 分钟在脑里做"训练史回顾 + 原理参照"。AI 能压到 30 秒。
- H2：教练课后想给会员发文字反馈但写不出来。AI 帮起草。
- H3：教练遇到不熟悉的会员状况（孕期 / 术后 / 老年）会犹豫，AI 多流派视角能拓宽思路。
- H4：会员级数据 + AI 是教练为何换我们而不是用 ChatGPT 的关键差异点。
- H5：AI 输出按教练真实记录格式（动作清单 + 强度% + 结构化字段），而不是描述性建议。
   是"用 vs 不用"的决定性因素。

---

## 二、Who · 目标用户

### 主用户：私教 / 工作室教练

- 年龄 25-45，主力女性
- Stott / Polestar / BASI 等主流流派受训背景者，或刚入行不久
- 平均每天 5-15 节课，会员数 20-100
- 微信小程序熟练用户，对 AI 不陌生但还在尝试期

### 次用户（待定）：会员

- **当前不是直接用户**
- 但教练可能截图 / 转述 AI 输出给会员，所以输出文风要照顾"教练→会员"传递场景

---

## 三、What · 核心功能（Phase A）

### 功能 1：AI 备课建议（必做）

**入口**：会员详情页底部 "用 AI 备课 →" 按钮

**输入**：
- 自动注入：该会员档案 / 近 5 节课 / 训练分布 / 客档（aiDigest）
- 手动输入：教练当前关注的问题（自由文本，可空）

**处理**：
- 4 流派 agent 并行分析（经典 / 当代 / 康复 / 健身）
- 1 裁判 agent 综合判断 + 给具体方案

**输出**：
- 4 派视角（默认折叠，可展开）
- 综合建议：训练重点 / 推荐动作家族 / 注意事项 / 时长强度
- 一键"应用到下次课程"（跳转到新建课程页，预填字段）

### 1.x 输出格式标准（参考教练真实工作流）

AI 备课建议应输出与教练真实手写记录一致的结构化格式，以便"应用到下次课程"时无需重新整理。

**Session 完整字段（V2 目标模型）**：

| 字段 | 类型 | 必填 | 说明 | 示例 |
|------|------|------|------|------|
| `sessionNumber` | int | 否 | 该会员累计第几节课。**可由系统建议**（已上完节数+1）**但允许覆盖**（线下历史数据可能未录入）| 16 |
| `memberState` | string | 否 | 课前/课中观察记录，自由文本，可多字 | "良好"、"今天明显焦虑，左肩紧张" |
| `focusArea` | string | 否 | 练习部位，自由文本，可单可多 | "骨盆带" |
| `trainingItems` | string[] | 否 | 训练内容，有序动作清单。每项是教练自由文本（包括"R"等自定义前缀，系统不解析） | ["坐姿髋部灵活", "单腿屈伸髋", "R 直背后倒"] |
| `courseType` | string | 是 | 运动方式（普拉提/瑜伽/体能等）| "普拉提" |
| `intensity` | int | 否 | 运动强度百分比 0-100 | 60 |
| `trainingResult` | string | 否 | 课后填写，自由文本 | "完成度高，但腰部代偿明显" |
| `summary` | string[] | 否 | 课程总结，要点列表 | ["仍存在两侧不平衡问题", "没有反应腰部代偿"] |
| `homework` | string[] | 否 | 家庭作业/课中记录，要点列表。**结构不固定**（每个馆/教练可能格式不同） | ["蜡笔小新走"] |
| `createdBy` | string | 否 | 创建/修改人，多人协作场景显示 | "天琪-83645" |

**示例（一节真实记录）**：

```yaml
date: 2026-05-14
sessionNumber: 16
memberState: 良好
focusArea: 骨盆带
trainingItems:
  - 坐姿髋部灵活
  - 单腿屈伸髋
  - 双腿交错平衡练习
  - 步态基础
  - R 直背后倒
  - R 侧链
  - R 滑板滑板车
courseType: 普拉提
intensity: 60
trainingResult: (留空，课后补)
summary:
  - 仍存在两侧不平衡问题
  - 没有反应腰部代偿
homework:
  - 蜡笔小新走
```

**关键设计原则**：

1. **不限制自由度**：`memberState` / `homework` / `trainingItems` 都是自由文本，因为不同教练 / 不同馆的工作流差异大
2. **AI 输出 vs 教练填写**：AI 备课建议生成的是**草稿默认值**，教练随时可改
3. **课时序号智能建议**：系统根据已有数据建议默认值，但允许手动覆盖（兼容线下未录入数据）
4. **训练结果留空可接受**：课前/课后两次填写场景

### 功能 2：多轮跟进（必做）

教练拿到建议后，可以追问：
- "如果时间只有 30 分钟呢？"
- "她说昨天腿酸"
- "下周三再上一节，重点要变吗？"

会话保持上下文，agent 基于已有共识继续推理，不重新分析全部。

### 功能 3：保存到客档（可选，Phase A 末或 Phase B）

把这次咨询的综合建议保存到该会员的客档时间轴，跟课程并列显示。

### 功能 4：通用咨询（不做 Phase A）

> 不依附会员的、纯原理咨询（"解释下骶骨稳定原理"）。
> 这种用 ChatGPT/Kimi 就够了，不是我们的差异化。

---

## 四、What NOT · 明确不做

| 不做 | 原因 |
|------|------|
| 通用普拉提百科问答 | 通用大模型已经够用，不是我们差异点 |
| AI 替教练做最终决定 | 我们是辅助，不是替代。所有输出标"教练审核后采纳" |
| 实时视频动作纠正 | 视觉 + 实时反馈 = 完全不同的产品形态，远期再说 |
| 会员直接用 AI 自己练 | 法律责任 + 安全风险高，远期且要专门评估 |
| 跨教练的会员对比 | 数据隔离原则，每个教练的会员数据独立 |
| RAG 检索教材原文 | IP 风险，已用 llm-wiki 蒸馏方案规避 |
| ReAct 全套自主 agent | 我们是 pipeline 编排，不让 agent 自由发挥 |

---

## 五、成功标准

### Phase A 验证条件（全部满足才算 PMF）

| 维度 | 指标 |
|------|------|
| 使用率 | 内测 5 个教练中至少 3 个，每周用 ≥ 3 次 |
| 时间节省 | 教练自评：用前 vs 用后，备课时间缩短 50%+ |
| 输出可用率 | 综合建议 "一键应用到课程" 命中率 ≥ 60%（教练采纳大部分字段不大改）|
| 维持留存 | 4 周后仍在用，没回退到不用 AI 的状态 |

### 反向指标（任何一项触发就要重新审视）

- 单次咨询成本 > ¥1
- 用户主要在用通用咨询而不是基于会员的咨询（说明定位错了）
- 有教练抱怨 AI 输出"误导他做错决定"（说明免责 + UX 不到位）

---

## 六、关键约束

- **预算**：单次咨询 LLM 成本应 ≤ ¥0.10（4 派 + 裁判 + 多轮 1-2 次跟进）
- **延迟**：首屏 4 派意见 ≤ 8 秒（流式可见），裁判 ≤ 总 15 秒
- **可靠性**：99% 可用（agent 降级要优雅）
- **合规**：不涉及医疗诊断 / 不替代专业意见 / 必带免责声明
- **隐私**：会员数据不出我们后端 + LLM 调用（DashScope/DeepSeek 对企业数据有政策）
- **可持续**：知识层（wiki）独立于 agent，可任意更新不影响 agent 代码

---

## 七、待回答的核心问题（多轮访谈 / 内测时定）

> 这些问题不在文档里现在回答，留给真实用户测试时反馈。

1. 入口位置 = 会员详情页吗？还是周视图卡片长按多一项 "AI 备课"？
2. 跟现有 aiDigest（每节课客档）的关系：AI 备课建议要不要参考 aiDigest 全文？
3. 输入框默认 placeholder 是什么？要不要预设 prompt 模板（"评估近期表现" / "下次重点哪里"）？
4. 输出 4 派是默认全展开（信息密度高，给老用户）还是默认折叠（轻量，给新用户）？
5. "一键应用到课程" 应用哪些字段？只 focusAreas + notes？还是包括 duration / location？
6. 多轮对话状态保留多久？同会员同教练的会话是否永久保留？
7. 失败时（LLM 超时 / 知识层缺失）UI 怎么表现？给降级回答还是直接报错？
8. 移动端 vs PC 端：会员详情页 PC 上的展开方式？
9. 离线场景：教练在地铁里没网，能否打开历史咨询？
10. 数据安全：教练删除会员时，相关 AI 咨询历史是否一并删除？

---

## 八、版本规划

```
v0.1（当前文档）─── 写出基础假设、留白待访谈

v0.2 ───── 跟用户访谈完，调整核心假设、补具体场景

v0.3 ───── 跟开发对齐，根据技术预研调整范围

v1.0 ───── 进入实施期前的冻结版本
```

---

## 九、Session 数据模型演化（路径 2 决策）

### 决策

**V1 不动数据模型，V2/Phase 3 一起升级**。当前已发布的小程序数据继续兼容。

### V1（当前生产中）

```typescript
Session {
  id, memberId, memberIds[],
  date, startTime, duration,
  classMode, courseType, location, status,
  focusAreas: string[],   // 多标签
  notes: string,           // 自由文本（混杂了总结/作业/观察等）
  photos, beforePhotos, afterPhotos,
  summaryText, summarySent,
  voiceSegments, aiDigest,
  createdAt, updatedAt
}
```

### V2 目标（Phase 3 同步上）

```typescript
Session {
  // 保留的 V1 字段
  id, memberId, memberIds[],
  date, startTime, duration,
  classMode, courseType, location, status,
  photos, beforePhotos, afterPhotos,
  createdAt, updatedAt,

  // 新增字段（参考真实教练记录格式）
  sessionNumber: number?,     // 课时序号（系统建议+允许覆盖）
  memberState: string?,        // 会员状态（自由文本）
  focusArea: string?,          // 练习部位（自由文本，可单可多）
  trainingItems: string[],     // 训练内容（有序动作清单）
  intensity: number?,          // 运动强度 0-100
  trainingResult: string?,     // 课后训练结果
  summary: string[],           // 课程总结要点
  homework: string[],          // 家庭作业/课中记录
  createdBy: string?,          // 多人协作时

  // V1 字段在 V2 里的处理
  focusAreas: string[],        // 保留，跟 focusArea 并行（前者结构化标签，后者自由文本）
  notes: string?,              // 保留为"备注"兜底字段（如果新字段不够用）
  voiceSegments, aiDigest,     // 保留（AI 草稿源）
  summaryText, summarySent     // 保留（专门给"发给会员的文字版总结"用）
}
```

### V1 → V2 迁移策略

不需要复杂的数据迁移，因为：
- V2 新字段都是 optional，旧 Session 缺失时按 null/空数组对待
- V1 的 `notes` 文本字段保留，新字段提供更细分的存储

**可选迁移工具**（不强制）：
- 一次性脚本：用 LLM 把 V1 `notes` 拆解成 V2 的 `summary` + `homework` + `trainingResult`
- 用户在编辑课程时可选 "智能拆分备注到结构化字段"

### 对 V1 的反向影响

无。V1 用户继续用 `notes` 自由文本，不会被打扰。

### 对 AI Agent（Phase 3）的影响

- Agent 输出 schema 直接对齐 V2 结构（`memberState` / `trainingItems` / `intensity` / `summary` / `homework`）
- "应用到下次课程"映射到 V2 字段，无需中间翻译
- 教练审核 AI 输出时看到的是熟悉的"训练内容清单 + 强度% + 课程总结要点"，不是抽象描述
